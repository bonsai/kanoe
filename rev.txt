 (9)
Open
Add MediaPipe pose detection for head controls#1
bonsai
wants to merge 2 commits into
main
from
kanoe
Conversation
@bonsai
Owner
bonsai
commented
17 minutes ago
• 
Added MediaPipe pose detection to control the kanoe game with head movements for kids. Head down to duck, head left/right to steer.

Continue Tasks: ❌ 1 failed — View all

Summary by CodeRabbit
New Features
Introduced a browser-based 3D canoe expedition game with head gesture controls powered by pose detection.
Navigate bridges using head movements to duck and steer, or use keyboard controls.
Track score, speed, and altitude while paddling toward the sea with dynamic challenges and bridge obstacles.
Your Name added 2 commits 25 minutes ago
Add MediaPipe pose detection for head controls
88c0f3f
Merge main into kanoe branch
5adae6d
@gemini-code-assist
gemini-code-assist bot
commented
16 minutes ago
Summary of Changes
Hello, I'm Gemini Code Assist1! I'm currently reviewing this pull request and will post my feedback shortly. In the meantime, here's a summary to help you and other reviewers quickly get up to speed!

This pull request introduces an innovative control mechanism to the Kanoe game by integrating MediaPipe's pose detection capabilities. Players can now interact with the game using natural head movements, ducking under bridges by tilting their head down and steering the canoe by moving their head left or right. This enhancement aims to provide a more immersive and accessible gaming experience, particularly for younger players, by leveraging modern computer vision technology.

Highlights
MediaPipe Pose Detection Integration: Integrated MediaPipe for real-time head pose detection, enabling a new control scheme for the Kanoe game.
Head Movement Controls: Implemented game controls where head movements directly translate to in-game actions: head down to duck and head left/right to steer the canoe.
New Pose Controller Module: Introduced a dedicated PoseController class to manage MediaPipe setup, camera input, landmark visualization, and robust head gesture detection with smoothing.
Enhanced User Interface: Added visual feedback for pose detection status, a small camera feed for user reference, and on-screen hints for head controls.
Structured Bridge Data: Migrated bridge information from a CSV to a more structured JSON format, including additional details like elevation, and updated game logic to consume this new data.
🧠 New Feature in Public Preview: You can now enable Memory to help Gemini Code Assist learn from your team's feedback. This makes future code reviews more consistent and personalized to your project's style. Click here to enable Memory in your admin console.

Changelog
Using Gemini Code Assist
Footnotes
Review the Privacy Notices, Generative AI Prohibited Use Policy, Terms of Service, and learn how to configure Gemini Code Assist in GitHub here. Gemini can make mistakes, so double check it and use code with caution. ↩

@coderabbitai
coderabbitai bot
commented
16 minutes ago
• 
📝 Walkthrough
🚥 Pre-merge checks | ✅ 3
✨ Finishing Touches
Tip

Try Coding Plans. Let us write the prompt for your AI agent so you can ship faster (with fewer bugs).
Share your feedback on Discord.

Thanks for using CodeRabbit! It's free for OSS, and your support helps us grow. If you like it, consider giving us a shout-out.

❤️ Share
Comment @coderabbitai help to get the list of available commands and usage tips.

chatgpt-codex-connector[bot]
chatgpt-codex-connector bot reviewed 14 minutes ago
chatgpt-codex-connector bot
left a comment
💡 Codex Review
Here are some automated review suggestions for this pull request.

Reviewed commit: 5adae6db5d

ℹ️ About Codex in GitHub
pose-controller.js

        // Detect gestures
        const gesture = {
            isDucking: headOffsetY < -this.thresholds.headDown,
@chatgpt-codex-connector
chatgpt-codex-connector bot
14 minutes ago
P1 Badge Fix inverted ducking threshold

In MediaPipe pose coordinates, y increases downward, so a normal upright nose position is usually well above the shoulders (more negative headOffsetY), while ducking moves it toward zero. Using headOffsetY < -this.thresholds.headDown therefore marks many neutral poses as ducking and can clear ducking when the player actually lowers their head, making head-based duck controls behave opposite to the UI hint.

Useful? React with 👍 / 👎.

@bonsai	Reply...
script.js
Comment on lines +317 to +319
                    const newBridge = createBridge(
                        bridgeData[gameState.currentBridgeIndex],
                        bridge.group.position.z - gameState.bridgeSpacing
@chatgpt-codex-connector
chatgpt-codex-connector bot
14 minutes ago
P1 Badge Spawn the next unseen bridge instead of current index

createInitialBridges() already places the first three entries, but after a pass this code spawns bridgeData[currentBridgeIndex] again. After bridge 0 is cleared, currentBridgeIndex becomes 1 and a second copy of bridge 1 is created at the same Z as the preloaded one, which can trigger duplicate pass handling in one approach (extra score/index jumps and out-of-order progression).

Useful? React with 👍 / 👎.

@bonsai	Reply...
script.js
        });
    }
    // スコア更新
    gameState.score += Math.floor(gameState.speed * 0.1);
@chatgpt-codex-connector
chatgpt-codex-connector bot
14 minutes ago
P2 Badge Accumulate fractional speed score before flooring

This update always adds zero because speed is capped at 5.0, so Math.floor(gameState.speed * 0.1) is always 0 (0.1–0.5 floored). As a result, the per-frame score path never contributes and score only changes on bridge bonuses, which contradicts the intended continuous score update.

Useful? React with 👍 / 👎.

@bonsai	Reply...
gemini-code-assist[bot]
gemini-code-assist bot reviewed 11 minutes ago
gemini-code-assist bot
left a comment
Code Review
This pull request introduces an impressive new feature: controlling the game using head movements via MediaPipe pose detection. The implementation is quite comprehensive, adding several new JavaScript files for game logic, the pose controller, and control integration. The code is generally well-structured. I've identified a few areas for improvement, including a critical race condition in initialization, a bug related to duplicate DOM element IDs, potential memory leaks, and several opportunities to enhance code quality by removing magic numbers and inline styles. Addressing these points will significantly improve the robustness and maintainability of the new feature.

head-controls.js
Comment on lines +64 to +67
setTimeout(() => {
    setupHeadControls();
    console.log('Head controls initialized');
}, 1000);
@gemini-code-assist
gemini-code-assist bot
11 minutes ago
high

Using setTimeout to initialize the head controls is fragile and can create a race condition. If the poseController takes longer than one second to initialize, the controls will not be set up. A more robust solution is to use an event-driven approach. The pose-controller.js can dispatch a custom event when it's ready, and this script can listen for that event. I've added a corresponding suggestion in pose-controller.js.

Suggested change
setTimeout(() => {
    setupHeadControls();
    console.log('Head controls initialized');
}, 1000);
// Wait for game to load and setup head controls
window.addEventListener('poseControllerReady', () => {
    setupHeadControls();
    console.log('Head controls initialized');
});
@bonsai	Reply...
pose-controller.js
Comment on lines +107 to +108
                    this.updateStatus('📷 ポーズ検出：準備完了', 'green');
                    resolve();
@gemini-code-assist
gemini-code-assist bot
11 minutes ago
high

To prevent a race condition with head-controls.js, it's best to notify other scripts when the pose controller is fully initialized. Dispatching a custom event here ensures that dependent scripts can reliably set up their logic only after the camera and pose detection are ready.

                    this.updateStatus('📷 ポーズ検出：準備完了', 'green');
                    window.dispatchEvent(new Event('poseControllerReady'));
                    resolve();
@bonsai	Reply...
script.js
Comment on lines +541 to +544
const seaAnimation = document.createElement('div');
seaAnimation.id = 'seaAnimation';
seaAnimation.innerHTML = '<div class="sea-text">海に到着！<br>おつかれさまでした</div>';
document.body.appendChild(seaAnimation);
@gemini-code-assist
gemini-code-assist bot
11 minutes ago
high

This code creates and appends a new div with id="seaAnimation". However, an element with this ID already exists in index.html. This results in invalid HTML with duplicate IDs and can cause unpredictable behavior. The script should get a reference to the existing element instead of creating a new one.

const seaAnimation = document.getElementById('seaAnimation');
@bonsai	Reply...
.picasa.ini
Comment on lines +1 to +4
[Picasa]
P2category=Exported Pictures
date=45830.086968
name=kanoe
@gemini-code-assist
gemini-code-assist bot
11 minutes ago
medium

This file appears to be automatically generated by Picasa photo software and contains local user metadata. It's generally not recommended to commit such files to a source code repository. Please remove this file and add .picasa.ini to your .gitignore file to prevent it from being committed in the future.

@bonsai	Reply...
head-controls.js
Comment on lines +37 to +60
    if (steeringState.left) {
        window.canoeGroup.position.x = Math.max(
            -35,
            window.canoeGroup.position.x - steeringState.speed
        );
        // Tilt canoe when steering
        window.canoeGroup.rotation.z = Math.min(
            0.3,
            window.canoeGroup.rotation.z + 0.05
        );
    } else if (steeringState.right) {
        window.canoeGroup.position.x = Math.min(
            35,
            window.canoeGroup.position.x + steeringState.speed
        );
        // Tilt canoe when steering
        window.canoeGroup.rotation.z = Math.max(
            -0.3,
            window.canoeGroup.rotation.z - 0.05
        );
    } else {
        // Return to center rotation
        window.canoeGroup.rotation.z *= 0.9;
    }
@gemini-code-assist
gemini-code-assist bot
11 minutes ago
medium

The code for applying steering contains several "magic numbers" for position limits, rotation limits, and damping factors. To improve readability and make these values easier to adjust, it's best practice to extract them into named constants at the top of the file.

const STEERING_X_LIMIT = 35;
const TILT_Z_LIMIT = 0.3;
const TILT_INCREMENT = 0.05;
const TILT_DAMPING_FACTOR = 0.9;

// Apply steering in game loop - called from animate()
function applySteering() {
    if (!window.canoeGroup || gameState.isGameOver) return;
    
    if (steeringState.left) {
        window.canoeGroup.position.x = Math.max(
            -STEERING_X_LIMIT,
            window.canoeGroup.position.x - steeringState.speed
        );
        // Tilt canoe when steering
        window.canoeGroup.rotation.z = Math.min(
            TILT_Z_LIMIT,
            window.canoeGroup.rotation.z + TILT_INCREMENT
        );
    } else if (steeringState.right) {
        window.canoeGroup.position.x = Math.min(
            STEERING_X_LIMIT,
            window.canoeGroup.position.x + steeringState.speed
        );
        // Tilt canoe when steering
        window.canoeGroup.rotation.z = Math.max(
            -TILT_Z_LIMIT,
            window.canoeGroup.rotation.z - TILT_INCREMENT
        );
    } else {
        // Return to center rotation
        window.canoeGroup.rotation.z *= TILT_DAMPING_FACTOR;
    }
@bonsai	Reply...
1 hidden conversation
Load more…
index.html
            <h2>ゲームオーバー!</h2>
            <p>橋に衝突してしまいました...</p>
            <p>最終スコア：<span id="finalScore">0</span></p>
            <button onclick="restartGame()" class="control-btn restart-btn">もう一度プレイ</button>
@gemini-code-assist
gemini-code-assist bot
11 minutes ago
medium

Using the onclick attribute is an older practice that mixes JavaScript logic into your HTML. A more modern and maintainable approach is to use addEventListener in your JavaScript file to attach event handlers to DOM elements. This improves separation of concerns.

Suggested change
            <button onclick="restartGame()" class="control-btn restart-btn">もう一度プレイ</button>
            <button class="control-btn restart-btn">もう一度プレイ</button>
@bonsai	Reply...
pose-controller.js
Comment on lines +152 to +159
        const connections = [
            [0, 1], [1, 2], [2, 3], [3, 7],  // Head
            [0, 4], [4, 5], [5, 6],          // Neck to shoulders
            [9, 10],                         // Shoulders
            [11, 12], [12, 14], [14, 16],   // Left arm
            [11, 13], [13, 15],              // Left arm continuation
            [12, 11],                        // Shoulder connection
        ];
@gemini-code-assist
gemini-code-assist bot
11 minutes ago
medium

The connections array for drawing pose landmarks contains some inaccuracies and misleading comments. For example, [9, 10] connects the mouth, not the shoulders, and [12, 11] is a redundant connection. Correcting these will improve the accuracy of the visualization and make the code easier to understand.

        const connections = [
            [0, 1], [1, 2], [2, 3], [3, 7],  // Left face outline
            [0, 4], [4, 5], [5, 6],          // Right face outline
            [9, 10],                         // Mouth
            [11, 12],                        // Shoulders
            [11, 13], [13, 15],              // Left arm
            [12, 14], [14, 16]               // Right arm
        ];
@bonsai	Reply...
pose-controller.js
Comment on lines +189 to +192
        const leftEye = landmarks[1];
        const rightEye = landmarks[2];
        const leftEar = landmarks[3];
        const rightEar = landmarks[4];
@gemini-code-assist
gemini-code-assist bot
11 minutes ago
medium

The variables leftEye, rightEye, leftEar, and rightEar are declared but never used in this function. This constitutes dead code and should be removed to improve code clarity. Additionally, the landmark indices assigned to them are inconsistent with the MediaPipe Pose documentation.

@bonsai	Reply...
script.js
Comment on lines +148 to +161
    function animateWaves() {
        const positions = waves.geometry.attributes.position;
        const time = Date.now() * 0.001;

        for (let i = 0; i < positions.count; i++) {
            const x = positions.getX(i);
            const z = positions.getZ(i);
            const y = Math.sin((x + time) * 0.5) * 0.1 + Math.cos((z + time) * 0.3) * 0.05;
            positions.setY(i, y);
        }
        positions.needsUpdate = true;
        requestAnimationFrame(animateWaves);
    }
    animateWaves();
@gemini-code-assist
gemini-code-assist bot
11 minutes ago
medium

The animateWaves function creates a separate animation loop with requestAnimationFrame. It's more efficient and better for synchronization to have a single main game loop. The wave animation logic should be integrated into the main animate() function.

@bonsai	Reply...
script.js
Comment on lines +412 to +414
    while(scene.children.length > 0) {
        scene.remove(scene.children[0]);
    }
@gemini-code-assist
gemini-code-assist bot
11 minutes ago
medium

The restartGame function removes objects from the scene but doesn't properly dispose of their geometries and materials. This can lead to memory leaks in WebGL, as these resources remain on the GPU. Over multiple restarts, this could degrade performance or cause the application to crash. You should traverse the scene and explicitly call .dispose() on these resources.

    while(scene.children.length > 0) {
        const child = scene.children[0];
        scene.remove(child);

        if (child instanceof THREE.Mesh) {
            child.geometry.dispose();
            // Handle arrays of materials
            if (Array.isArray(child.material)) {
                child.material.forEach(material => material.dispose());
            } else {
                child.material.dispose();
            }
        }
    }
@bonsai	Reply...
coderabbitai[bot]
coderabbitai bot reviewed 8 minutes ago
coderabbitai bot
left a comment
Actionable comments posted: 6

🧹 Nitpick comments (1)
🤖 Prompt for all review comments with AI agents
ℹ️ Review info
pose-controller.js
Comment on lines +227 to +231
        const gesture = {
            isDucking: headOffsetY < -this.thresholds.headDown,
            steerLeft: headOffsetX < -this.thresholds.headLeft,
            steerRight: headOffsetX > this.thresholds.headRight
        };
@coderabbitai
coderabbitai bot
8 minutes ago
⚠️ Potential issue | 🔴 Critical

Ducking predicate is inverted (detects head-up as duck).

headOffsetY is nose minus shoulder-center Y. Ducking moves nose downward, so this value increases toward zero/positive. Current check uses < -threshold, which maps the opposite behavior.

🐛 Proposed fix
🤖 Prompt for AI Agents
@bonsai	Reply...
script.js
Comment on lines +148 to +161
    function animateWaves() {
        const positions = waves.geometry.attributes.position;
        const time = Date.now() * 0.001;

        for (let i = 0; i < positions.count; i++) {
            const x = positions.getX(i);
            const z = positions.getZ(i);
            const y = Math.sin((x + time) * 0.5) * 0.1 + Math.cos((z + time) * 0.3) * 0.05;
            positions.setY(i, y);
        }
        positions.needsUpdate = true;
        requestAnimationFrame(animateWaves);
    }
    animateWaves();
@coderabbitai
coderabbitai bot
8 minutes ago
⚠️ Potential issue | 🟠 Major

Wave animation loop is duplicated on every restart.

createRiver() starts its own RAF loop every call, and restartGame() calls createRiver() again. Old loops continue running against orphaned geometry, causing avoidable CPU/memory growth.

⚙️ Proposed fix (store/cancel RAF id)
Also applies to: 442-443

🤖 Prompt for AI Agents
@bonsai	Reply...
script.js
Comment on lines +286 to +295
// 初期橋を作成
function createInitialBridges() {
    for (let i = 0; i < Math.min(3, bridgeData.length); i++) {
        const bridge = createBridge(
            bridgeData[i], 
            -gameState.bridgeSpacing * (i + 1)
        );
        bridges.push(bridge);
    }
}
@coderabbitai
coderabbitai bot
8 minutes ago
⚠️ Potential issue | 🔴 Critical

Bridge progression logic reuses already-instantiated bridges and can duplicate scoring.

After creating 3 initial bridges, spawn logic uses bridgeData[currentBridgeIndex] and passedBridge.z - spacing, which re-creates entries already on screen (same data and often same Z). This can trigger repeated pass handling in one frame and corrupt progression.

🐛 Proposed fix (track next spawn index + spawn behind farthest bridge)
Also applies to: 316-322

🤖 Prompt for AI Agents
@bonsai	Reply...
script.js
Comment on lines +297 to +309
// 3D衝突検出 → 幅内でしゃがみ判定
function checkBridgeDuck() {
    if (!window.canoeGroup) return false;
    const canoeZ = window.canoeGroup.position.z;
    for (let i = 0; i < bridges.length; i++) {
        const bridge = bridges[i];
        const bridgeZ = bridge.group.position.z;
        const bridgeWidth = 3; // 橋の奥行き幅（BoxGeometryのzサイズ）
        // 橋の幅の間に入ったか
        if (!bridge.passed && Math.abs(canoeZ - bridgeZ) < bridgeWidth/2) {
            if (!gameState.isDucking) {
                return true; // しゃがんでいない→ゲームオーバー
            } else {
@coderabbitai
coderabbitai bot
8 minutes ago
⚠️ Potential issue | 🟠 Major

checkBridgeDuck is not 3D collision and ignores bridge clearance height.

Current logic game-overs purely on isDucking when Z overlaps. It does not use canoe top Y vs bridge bottom Y, so very high bridges still require ducking.

🛠️ Proposed fix (use actual clearance check)
🤖 Prompt for AI Agents
@bonsai	Reply...
script.js
Comment on lines +541 to +544
const seaAnimation = document.createElement('div');
seaAnimation.id = 'seaAnimation';
seaAnimation.innerHTML = '<div class="sea-text">海に到着！<br>おつかれさまでした</div>';
document.body.appendChild(seaAnimation);
@coderabbitai
coderabbitai bot
8 minutes ago
⚠️ Potential issue | 🟡 Minor

Do not create a second #seaAnimation element.

index.html already defines #seaAnimation (Line 58 in index.html). Creating another node with the same id causes invalid DOM and ambiguous styling/selection.

🧩 Proposed fix
🤖 Prompt for AI Agents
@bonsai	Reply...
style.css
  overflow: hidden;
  padding: 0;
  background: #000;
  font-family: 'Arial', sans-serif;
@coderabbitai
coderabbitai bot
8 minutes ago
⚠️ Potential issue | 🟡 Minor

Use unquoted Arial to satisfy the lint rule.

Line 6 uses quotes around Arial, which triggers font-family-name-quotes.

🧹 Proposed fix
📝 Committable suggestion
🧰 Tools
🤖 Prompt for AI Agents
@bonsai	Reply...
Merge info
Some checks were not successful
1 failing, 2 successful checks


failing checks
Continuous AI: Refactorer
Continuous AI: Refactorer — Agent encountered an error
successful checks
CodeRabbit
CodeRabbit — Review completed
GitGuardian Security Checks
GitGuardian Security ChecksSuccessful in 15s — No secrets detected ✅
No conflicts with base branch
Merging can be performed automatically.

You can also merge this with the command line. 
Still in progress?
@bonsai


Add a comment
Comment
 
Add your comment here...
Remember, contributions to this repository should follow our GitHub Community Guidelines.
 ProTip! Add .patch or .diff to the end of URLs for Git’s plaintext views.
Reviewers
@coderabbitai
coderabbitai[bot]
@chatgpt-codex-connector
chatgpt-codex-connector[bot]
+1 more reviewer
@gemini-code-assist
gemini-code-assist[bot]

Still in progress?
Assignees
No one—
Labels
None yet
Projects
None yet
Milestone
No milestone
Development
Successfully merging this pull request may close these issues.

None yet


Notifications
Customize
You’re receiving notifications because you authored the thread.
1 participant
@bonsai
Footer
© 2026 GitHub, Inc.
Footer navigation
Terms
Privacy
Security
Status
Community
Docs
Contact
Manage cookies
Do not share my personal information